import { Segment, Dimmer } from "semantic-ui-react";

export const Loader = () => {
  <Segment>
    <Dimmer active inverted>
      <Loader inverted content="Loading" />
    </Dimmer>
  </Segment>;
};
