import { Segment, Dimmer } from "semantic-ui-react";

export const Loader = () => {
  <Segment>
    <Dimmer active inverted>
      <Loader size="large">Loading</Loader>
    </Dimmer>
  </Segment>;
};
